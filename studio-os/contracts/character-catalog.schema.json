{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opencitystudio.ru/schemas/character-catalog.schema.json",
  "title": "OpenCity character catalog",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "doc_kind",
    "doc_function",
    "context",
    "purpose",
    "canonical_for",
    "derived_from",
    "status",
    "schema_version",
    "catalog_id",
    "character_context",
    "characters"
  ],
  "properties": {
    "doc_kind": { "const": "canon" },
    "doc_function": { "const": "canonical" },
    "context": { "const": "open_city_canon" },
    "purpose": { "type": "string", "minLength": 1 },
    "canonical_for": { "const": "open_city_world_character_profiles" },
    "derived_from": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "status": { "const": "active" },
    "schema_version": { "const": 2 },
    "catalog_id": { "const": "open-city-world-characters" },
    "character_context": { "const": "comic_world" },
    "characters": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/character" }
    }
  },
  "$defs": {
    "character": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "identity_id",
        "dossier_number",
        "name",
        "role",
        "dramatic_function",
        "system_note",
        "accent",
        "profile_status",
        "visual_status",
        "studio_chat_persona_id",
        "archive_visual_references",
        "appearances"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "identity_id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "dossier_number": { "type": "string", "pattern": "^CITY-[0-9]{3}$" },
        "name": { "type": "string", "minLength": 1 },
        "role": { "type": "string", "minLength": 1 },
        "dramatic_function": { "type": "string", "minLength": 1 },
        "system_note": { "type": "string", "minLength": 1 },
        "quote": { "type": ["string", "null"] },
        "accent": { "enum": ["cyan", "magenta", "orange", "teal"] },
        "profile_status": { "enum": ["active", "announced"] },
        "visual_status": { "enum": ["approved", "awaiting_master"] },
        "studio_chat_persona_id": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "archive_visual_references": {
          "type": "array",
          "items": { "$ref": "#/$defs/archiveVisualReference" }
        },
        "appearances": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^issue-[0-9]{3}$" }
        }
      }
    },
    "archiveVisualReference": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "artifact_path",
        "conversation_id",
        "context",
        "reference_type",
        "status",
        "note"
      ],
      "properties": {
        "artifact_path": {
          "type": "string",
          "pattern": "^knowledge/sources/chatgpt/open-city/archive/artifacts/.+\\.png$"
        },
        "conversation_id": {
          "type": "string",
          "pattern": "^[a-f0-9-]+$"
        },
        "context": { "enum": ["comic_world", "studio_chat", "shared_identity"] },
        "reference_type": {
          "enum": ["character_sheet", "mini_avatar", "profile_card", "style_iteration", "ensemble_sheet"]
        },
        "status": { "const": "source_only" },
        "note": { "type": "string", "minLength": 1 }
      }
    }
  }
}
