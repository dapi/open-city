---
doc_kind: process
doc_function: canonical
context: episode_production
purpose: Default episode lifecycle.
status: active
---

# Episode production

`brief → draft script → approved script → voice → animation → render → release → retro`

Every transition writes or updates a manifest. The next agent consumes the manifest,
not an informal description in chat.
